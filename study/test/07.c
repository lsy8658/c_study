#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
// ----------------------------------


// ----------------------------------

int fncMath () {
  double x = 12.34;
  int i = -5;
  int a,b,c,d,e,f;
  a = (int) ceil(x); // ????
  b = (int) floor(x); // ????
  c = (int) pow(4, 2);

  printf("abs(-5) = %d\n", abs(i));
  printf("ceil => %d\n", a);
  printf("floor => %d\n", b);
  printf("cos => %f\n", cos(10));
  printf("exp => %f\n", exp(2));
  printf("sqrt => %d\n", sqrt(2));
  printf("pow => %d\n", c);
};
// ----------------------------------

int fncStr () { 
  // isalpha : ??? ???(A–Z, a–z) ?? ??? ? ??
  // isdigit : ? ??? ?????

}

// ----------------------------------

// ----------------------------------

// ----------------------------------

// ----------------------------------

// ----------------------------------
int main () {
 
}

// ----------------------------------
